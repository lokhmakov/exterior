import createModule         from 'core/redux/api/createModule'


import start                from './start'
import finish               from './finish'


export default () => createModule({
  name: `mutation`,
  override: {
    reduxList: [
      start,
      finish,
    ]
  }
})