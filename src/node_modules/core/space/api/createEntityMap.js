import createModule         from 'core/redux/api/createModule'


import createModuleQuery    from 'core/query/api/createModule'
import createModuleMutation from 'core/mutation/api/createModule'


export default () => {
  return {
    item: createModule({
      name: `item`,
    }),
    query: createModuleQuery(),
    mutation: createModuleMutation(),
  }
}
