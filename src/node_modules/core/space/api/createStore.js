import createDebugger       from 'core/debug/api/create'

import createStore          from 'core/redux/api/createStore'

import createEntityMap      from './createEntityMap'


const debug = createDebugger(`core:space:api:createStore`)

export default () => {
  debug(`()`)
  return createStore(createEntityMap())
}
