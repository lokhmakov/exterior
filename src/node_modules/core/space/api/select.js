import { createSelector }   from 'reselect'


const selectNs = (
  state,
  { ns = `default` }
) => state.item[ns] || {}

export default createSelector(
  selectNs,
  (state, props) => props,
  (namespace, { entity }) => namespace[entity] || {},
)
