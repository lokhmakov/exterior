import React, { Component } from 'react'
import { withRouter }       from 'react-router-dom'

import ProjectListSection   from 'app/project/web/ProjectListSection'
// import withQuery            from 'core/query/api/hoc/withQuery'

const PageHome = ({ history }) => {
  return (
    <div>
      <h1>Главная</h1>
      <ProjectListSection
        onClick={({ id }) => history.push({
          pathname: `/editor/${ id }`,
        })}
      />
    </div>
  )
}

export default withRouter(PageHome)