import createDebugger       from 'core/debug/api/create'


const debug = createDebugger(`app:project:api:local:update`)

export default (payload) => {
  debug(`()`, payload)

  return {
    data: {
      [payload.id]: payload,
    },
    order: [payload.id],
  }
}