import React, { Component } from 'react'


import ProjectListItem      from './ProjectListItem'


export default ({
  data,
  order,

  onClick,
}) => {
  return (
    <div>
      {order.map((id) => (
        <ProjectListItem
          {...data[id]}

          onClick={ onClick }  
        />
      ))}
    </div>
  )
}